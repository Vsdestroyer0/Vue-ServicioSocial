# Documentación del sistema

Esta guía documenta los componentes Vue reutilizables de la aplicación.

La documentación de cada componente se encuentra en la carpeta `document` y se vincula automáticamente con su archivo `.vue` correspondiente.
